# 当前进展

更新时间：2026-07-19

## 当前版本

1.2.3。扩展通用识别固定字节分片，并为只暴露 blob/分片的页面建立稳定内容页解析候选；桌面用固定 yt-dlp/Deno 解析完整媒体，FFmpeg 继续独占下载、合并、校验、保存与 Eagle 导入。

## 1.2.3 通用页面解析与 Instagram 分片修复

现场 Instagram 页面共观测到 121 个视频请求，地址使用 `bytestart/byteend`。失败任务的 172,979 字节与 `886–173864` 区间完全相等，证明旧版把单个播放分片当作完整 MP4；去掉这两个传输参数并保留其他签名参数后，同一地址可按标准 Range 返回文件数据。

1.2.3 在扩展和桌面增加双层通用区间门禁；Instagram 元数据适配只负责按资产/轨道归组和估算完整大小。blob 播放器使用视频附近的同源稳定内容链接形成 `resolver=page` 候选，附真实当前帧、标题、时长和尺寸；链接规范化回归覆盖 Instagram、TikTok、抖音、X、Reddit、Facebook Watch 与 Pinterest。桌面解析器只接受公网 HTTP(S) 页面，Cookie 最多 16 KiB、只进任务内存和任务专用临时 cookie 文件，解析结束必删。存在页面解析候选时，重建分片地址只在技术分片开关中出现，默认列表不再堆叠小文件假视频。

102 项全量回归、4 组 Node 逻辑、全部扩展 JavaScript、双清单、冻结运行时和隔离安装器均通过；当前电脑已覆盖安装且在线健康门全绿。最终 ZIP 为 164,398,184 字节，SHA-256 `a0b038d7763fe087e59e908845182cb59fe39b5453fa0d8692f8a6c6ec0e8d9d`。Windows Computer Use 因无法高置信确认受保护 Chrome 扩展页 URL 而安全停止，因此 A163 只剩用户手动重载扩展后的 Instagram 现场下载一致性确认。

## 1.2.2 YouTube SABR 全画质

现场响应已确认包含 56 条 `adaptiveFormats`，覆盖 1440p、1080p、720p、480p、360p、240p、144p，但这些条目没有 `url` 或 `signatureCipher`，只有一个 360p progressive 条目带直链，并另给 `serverAbrStreamingUrl`。因此 1.2.1 只能安全显示 360p，问题不在 UI 筛选或去重。

1.2.2 把无直链的 SABR 元数据目录建模为 `resolver=youtube` 候选，popup 显示当前视频的全部唯一高度并默认选最高档。提交后桌面使用 yt-dlp 2026.06.09 + Deno 2.8.1 解析准确高度与最佳音轨，再把瞬时直链交给 FFmpeg。YouTube 请求 Cookie 只按当前标签页在扩展会话内暂存；桌面解析时生成任务归属 cookie 文件，解析完成或失败都立即删除，数据库、命令行、任务错误和诊断不含 Cookie。

97 项完整回归、冻结运行时、隔离安装器的全新安装/更新/故障回滚/卸载以及当前电脑覆盖安装均通过；在线 `/health` 为 1.2.2、`mediaReady=true`、`youtubeResolverReady=true`。最终 ZIP 为 164,373,275 字节，SHA-256 `7be6eed0ace15cced6d05b28ddf75cfe189a7d9ad3e9e6e73d56dc98c2a81864`。Chrome 受保护扩展页只能人工点击重载，因此只剩现场全画质目录与真实 1440p 下载闭环。

## 1.2.1 YouTube 结构化捕获

根因已复现：YouTube 使用 Media Source Extensions，DOM `video.currentSrc` 是页面同源占位地址，`webRequest` 只稳定暴露小型 `/videoplayback?...range=...` 探测，因而旧版显示四条约 7 KB 音频，无法形成 IDM 可见的 1440p–144p 目录。

1.2.1 新增独立 YouTube 主世界适配器，读取初始播放器响应并监听 `/youtubei/v1/player`，将带直接 URL 的 `adaptiveFormats` 按 videoId 归组、按 itag 区分，附带标题、缩略图、时长、分辨率、FPS、编码、码率和预计大小。通用归组同时修正同一路径不同 itag 被折叠、显式 Range 使用总文件大小时未判为分片，以及无组身份的小于 128 KB 音视频探测未隐藏的问题。95 项全量测试、JS/双清单、冻结运行时、隔离安装器、最终发行构建和当前电脑覆盖安装均通过；因 Chrome 禁止自动操作内部扩展页，只待用户点击一次“重新加载”后的现场页面确认。

## 1.2.0 职责收口与补导

修复前回归分别证明：候选仍按最新优先导致打开停在顶部、`segmentOnly` 当普通大卡展示、`MediaCoordinator` 没有本地完成文件补导 Interface、扩展仍装载下载/解析/录制/在线工具和旧图资源。修复后 `partitionGroups/defaultActiveGroupId`、`import_completed_plan`、`/api/media/import` 与覆盖安装清理清单均有自动门禁。

活动扩展从历史 96 文件工具箱收敛为必要文件，只保留普通/增强发现、B 站与 YouTube 结构化适配、内容预览、媒体候选组、认证提交和状态 UI。删除项被放入本机临时隔离目录以便本次工作恢复，未触碰用户下载、数据目录或 Eagle。

## 1.1.12 固定 Range 分片门禁

扩展通用解析 URL 查询参数、路径明文和 base64url 路径段中的 `range/bytes=start-end`；只有区间长度与已知候选大小一致时才判定固定分片。同组有 HLS/DASH 时分片不进入版本选择器，只有分片时组状态为 `segmentOnly`，计划创建前返回 `segment_only`。桌面 `MediaCoordinator` 同样在建库和重试执行前拒绝这类 URL，错误码为 `fixed_range_fragment`，避免旧扩展绕过。

修复前真实数据库稳定记录两条 `desktop_download_failed/2%`，定向 Node 与 Python 回归也分别失败；实现后扩展阻断和本机拒绝均通过。92 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器四路径和当前电脑覆盖安装均通过；在线 `/health` 为 1.1.12，安装目录关键扩展文件与三个可执行文件和发行包哈希一致。ZIP 为 93,415,850 字节，SHA-256 `298e46add8bd8699c4f4302c2f5f23073f0b6a04bf985a362aed0c385e7df5b3`。剩余 A144 为 Chrome 重载后的 Behance 现场闭环。

## 1.1.11 CDN 备用链接归并

归并仅限同一标签页和来源网站、同媒体类型，并要求响应 MD5/Digest/强 ETag 相同，或 URL 长路径相同且总字节数完全一致。文件大小本身不能作为归并依据；相同大小但路径/响应身份不同的媒体继续分开。显式播放器组与无元数据备用请求只有在强别名唯一指向该组时才合并，歧义时保持隔离。

修复前回归稳定复现为 2 个内容组；实现后变为 1 个内容组、1 个可下载版本，真实标题和预览随更丰富的播放器记录保留。不同路径的同大小媒体仍为 2 组，响应指纹相同但 CDN 路径不同的地址也能安全归并。91 项全量测试、冻结运行时、隔离安装器四路径和当前电脑覆盖安装已通过；在线 `/health` 为 1.1.11，安装目录关键扩展文件与三个可执行文件哈希一致。ZIP 为 93,404,556 字节，SHA-256 `29799b06ae5c695dfc8b08ea548da0b79b4d837b3d291900c64390d94181c39b`。剩余 A142 为 Chrome popup 肉眼确认和真实下载闭环。

## 1.1.10 候选自身直链预览

当前抖音页面现场只有 1 个可见 `<video>`，网络层却捕获 4 个媒体直链；因此 1.1.9 的精确匹配策略只能安全绑定 1 张页面播放器帧。新增 `previewMediaUrl(group, selection)` 只返回当前选择中的 `http/https` 视频 URL；popup 无静态帧时以 `muted + playsinline + preload=metadata` 的视频元素解码该 URL，并在元数据到达后定位到开头代表帧。切换候选版本会同步更换预览源，加载错误才显示占位图。

修复前 Node 回归因缺少 `previewMediaUrl` 失败；实现后候选自身 URL、版本切换和音频排除均通过。91 项全量 unittest、全部扩展 JavaScript 语法和双清单已通过。1.1.10 冻结运行时、隔离安装器的新装/升级/故障回滚/卸载和当前电脑覆盖安装均通过；在线 `/health` 为 1.1.10、协议 1、schema 5、`desktop_ffmpeg` 且媒体工具就绪，安装目录三份预览修复文件及三个可执行文件哈希一致。最终 ZIP 为 93,394,951 字节，SHA-256 `b4def98ef97ec47d5406503dbec3a19c09132c128520b92d174eaa9eb366d8b4`。剩余 A140 为 Chrome 手动重载后的多候选画面与下载内容闭环。

## 1.1.9 媒体身份一致性

现场数据库记录所选流 `duration=2713.034`，最终文件 FFprobe 实测 `708.141475`；本机预览首帧与 popup 所见内容不同，证明错配在创建方案前发生。根因是内容脚本在请求 URL 无法匹配任何 `<video>` 来源时，仍把当前播放/可见播放器作为回退，把它的帧、时长和稳定组键贴到后台预加载请求。

新增 `resolveVisualMatch` 以“同主机 + 同路径、忽略签名查询”匹配请求与播放器；未匹配请求返回 `none`，后台禁止复制其视觉身份。桌面 `MediaCoordinator` 对非清单直链比较声明时长与 FFprobe 输出时长，超过 8% 或 3 秒容差时以 `output_duration_mismatch` 失败并删除程序临时输出。回归在修复前分别因缺少匹配函数、旧回退逻辑和错误文件被正常交付而失败，修复后通过。

91 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器的新装/升级/故障回滚/卸载和当前电脑覆盖安装均已通过。当前 `/health` 为 1.1.9、协议 1、schema 5、`desktop_ffmpeg` 且媒体工具就绪；安装目录三个身份修复扩展文件和三个可执行文件与源码/发行包哈希一致。最终 ZIP 为 93,384,445 字节，SHA-256 `609e2dd3673f8a454d3106ac597368825145f8a36f7d982471c1809cce0fe969`。Chrome 安全策略禁止自动进入扩展内部页，仍需用户手动重载后完成 A138 的真实同内容下载。

## 1.1.8 认证读取传输兼容（实机内容闭环未通过）

旧现场可稳定复现为：相同令牌携带正确扩展 Origin 的 GET 返回 200，缺少 Origin 的 GET 返回 401；Chrome 实机的配对 POST 成功后马上触发 401。新增回归先证明认证 POST 媒体健康接口为 404、扩展仍调用 GET，实现后健康和任务列表 POST 均通过。创建、停止、重试和打开目录原本已经使用同一 POST 认证语义。

90 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器的新装/升级/回滚/卸载和当前电脑覆盖安装均已通过。当前安装的认证 POST 健康接口返回 200，五个扩展关键文件与源码哈希一致；真实抖音任务虽完成为 `imported/100`，但内容身份错误，A136 未通过，由 1.1.9 接替。1.1.8 ZIP 为 93,372,384 字节，SHA-256 `d0d1ae902a63e1a4155179561a87d2e074586627158f0be3ec16cdf0aac15571`。

## 1.1.7 状态写入原子性（实机未通过）

配对令牌、`pendingEvents`、`lastPlanId` 和 `lastPlanStatus` 不再由互相独立的 `get → merge → set` 操作更新。`createStateUpdateQueue` 保证每次操作读取前一个操作提交后的最新快照；函数式更新让来源事件追加/消费和令牌条件清除在同一临界区计算。并发 Node 回归先因缺少队列失败，实现后稳定通过。

89 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器的新装/升级/回滚/卸载和当前电脑覆盖安装均已通过；真实 Chrome 配对仍因受认证 GET 401 失败，A134 未通过，由 1.1.8 接替。1.1.7 ZIP 为 93,369,775 字节，SHA-256 `b4c8bbbca54e192889f7e95a014915abf70e5a0c096ef7d036de1845d35a1bbd`。

## 1.1.6 配对竞态与持久连接（实机未通过）

当前 Chrome `Default` 配置加载仓库开发副本，扩展 ID 为 `phochakdmmmehfamipieafeiigencilf`。扩展存储最近记录呈现 43 字符令牌与空令牌交替；最后一个非空令牌的 SHA-256 与服务端当前 `extension_token_hash` 完全一致，服务端 Origin 也与该扩展 ID 一致，证伪了令牌内容和 Origin 不匹配。真正断点是配对前请求的迟到 401 覆盖了新状态。

`EagleBridgeAuthLogic.unauthorizedAction` 已独立覆盖无令牌旧请求、旧令牌请求、当前令牌确实失效三类情况。设置页保存令牌后调用受认证健康接口并再次读取认证状态。真实 Chrome 重载后仍发现来源/任务状态的旧写入可覆盖令牌，因此 1.1.6 不通过 A132，由 1.1.7 接替。

## 1.1.5 动作真实性与配对状态

当前 Chrome `Default` 配置确认加载路径为 `<仓库>\chrome-extension`，扩展 ID 与桌面记录的配对 Origin 一致；仓库 `bootstrap.js` 为空是防止本机密钥进入源码/发行源的预期安全行为。正式安装目录副本的 64 字符一次性凭据与本机恢复文件哈希匹配且未过期。点击故障发生后 SQLite `download_plans` 仍只有 8 条，最新仍是旧的 `completed_local/100` Tmall 任务，证明请求在写入计划前已被前端拦截。

代码已增加可独立测试的 `startValidatedTask` 边界：无效校验不会调用计划创建器，成功提示依赖返回的计划对象；单项/批量“仅下载”在未配对时禁用。service worker 新增显式 `autoPair` 消息，popup 每次连接检查都会先尝试恢复，再决定显示“需要配对”。完整构建、安装与 Chrome 实机下载验收见 [1.1.5 验证报告](docs/RELEASE_VERIFICATION_1.1.5.md)。

88 项全量回归、全部扩展 JavaScript/双清单、冻结运行时、隔离全新安装/升级/故障回滚/卸载和当前电脑覆盖安装均通过。当前 `/health` 为 1.1.5、schema 5、协议 1、`desktop_ffmpeg` 且媒体工具就绪；源码与安装目录四个关键扩展文件哈希一致。Chrome 扩展卡已从 1.1.4 重载为 1.1.5。最终 ZIP 大小与 SHA-256 记录在独立验证报告中。Chrome 自动化安全策略拒绝直接进入扩展内部 popup，因此 A130 的最后一次配对点击与真实 `planId` 对照仍需用户在已打开的扩展 UI 中完成。

## 1.1.4 任务同步与通用预览状态

已确认 Tmall 示例页面暴露标准可见 `<video>`，桌面数据库中的同一任务为 `completed_local/100` 且已有真实 `preview_path`，原故障来自 popup 吞掉同步错误和未把瞬时/本机帧绑定到任务。新版在认证失效时自动恢复配对，任务轮询成功会恢复连接灯；失败时保留最后状态但加同步中断提示。任务创建时立即绑定当前视频帧，重开 popup 后通过认证回环接口读取本程序 `下载中转站/预览` 内的 PNG；输出目录同样只按任务 ID 打开已校验的 `已完成` 父目录。

88 项全量测试、冻结运行时、隔离全新安装/升级/故障回滚/卸载和当前电脑覆盖安装已通过。成功升级已实测轮换新的单次恢复凭据。当前后台 `/health` 与安装目录扩展清单均为 1.1.4，四个任务同步/UI 修复文件和源码 SHA-256 一致；旧 Tmall 任务仍为 `completed_local/100` 且保留最终路径与预览路径。最终 ZIP 大小与 SHA-256 见 [1.1.4 验证报告](docs/RELEASE_VERIFICATION_1.1.4.md)。

## 1.1.3 动态质量目录状态

已用 8 档 Vimeo、8 档通用 HLS、3 档通用 DASH 和 8 档桌面 ffprobe 节目映射覆盖“任意数量、按当前视频决定”的路径。清单无法读取或未声明分辨率时不生成猜测档位，继续显示自动质量并由本机安全选择。84 项全量测试、冻结构建、隔离安装器、当前电脑覆盖安装和发行哈希均已通过；仅 Chrome 工具栏 popup 同视口截图待人工点验。

## 1.1.2 质量与清单状态

Behance/Vimeo 实页确认播放器公开 `1080p、720p、540p、360p、240p` 五档 DASH 质量，同时网络层产生 21 个交错的 MP4 分片。新版从播放器结构化配置提取完整 HLS 主清单与质量目录；同组存在清单时隐藏非显式 MP4/音频分片，质量选择器只列五个可用档位并标记最高档为推荐。

当前源码 84 项自动测试通过，新增覆盖播放器配置解析、清单替换 21 个分片、质量排序/默认值及本机节目流选择。1.1.2 冻结构建、隔离安装器、当前电脑覆盖安装、发行哈希和 Behance 实页结构化质量目录均已通过；仅 Chrome 工具栏 popup 同视口视觉截图待人工点验，详见独立验证报告。

## 1.1.1 UI 与候选状态

任务 18–21 在 1.1.1 曾保留深搜、缓存、移动 UA、三类录制、疑似密钥和媒体控制；这些历史入口已由 1.2.0 任务 51 明确退役。稳定播放器归组、分片收敛与真实帧能力继续保留。

桌面软件的“媒体下载”和“IDM 导入记录”双标签、schema 5 与 `desktop_ffmpeg` 状态机不变。当前源码 84 项自动测试、真实普通直链、Referer/User-Agent 分轨、AES-128 HLS、字幕、仅下载、schema 迁移、补丁升级自重载、全部 JavaScript 语法和 Chrome/Firefox 清单检查通过。

1.1.1 源码与冻结健康门均为 `version=1.1.1`、`databaseSchema=5`、`extensionProtocol=1`、`downloadEngine=desktop_ffmpeg`、`mediaReady=true`。83 项测试、冻结载荷、当前电脑覆盖安装、废弃脚本清理和发行哈希已通过；仅 Chrome 工具栏 popup 最终视觉对照仍待人工点验，详见本轮验证报告。

## 1.0.0 集成状态

任务 01–14 的 1.0.0 历史能力已实现；浏览器/在线下载路线由 1.1.0 本机统一下载取代，缓存/录制/移动 UA/密钥/媒体控制又由 1.2.0 退役。捕获、HLS/DASH 发现、B 站分轨归组、安全边界、许可材料和安装回滚继续保留。

68 项自动测试通过；真实 FFmpeg AES-128 HLS 和分轨音视频合并测试通过；Apple 公共 HLS 与 B 站公共非 DRM DASH 均生成并探测出音频+视频样本。隔离安装、0.6.0 升级、故障回滚、卸载和冻结运行时验证纳入发行脚本。完整证据见 [1.0.0 验证报告](docs/RELEASE_VERIFICATION_1.0.0.md)。

1.0.0 当时未完成的缓存/录制肉眼复验不再构成 1.2.0 发布门；当前人工范围改为分片默认隐藏、最新项定位和本地完成文件补导。正式 GitHub Release 上传仍需项目所有者签字。

## 已完成并验证

- IDM 下载完成接收、SQLite 持久队列、Eagle 导入、离线重试、内容去重与无来源直接导入。
- Chrome 来源捕获、网站规则、离线事件补发、手动记录、单次忽略和持久配对。
- 可视化网站规则列表；右下角菜单可直接打开规则。
- 助手未运行时由 IDM 接收器静默启动；来源最多短暂等待 4 秒，不阻塞无来源导入。
- Windows Forms 原生托盘宿主，避免旧 Python 原生回调导致的 `0xc0000409` 崩溃。
- PyInstaller 独立后端包含 Python/Tcl/Tk；发行包不依赖接收者电脑的 Python。
- 一键安装器自动复制程序、配置 IDM、创建桌面/开始菜单入口、注册卸载项并启动助手。
- 安装器保护 IDM 既有杀毒软件配置，并保存原值供卸载恢复。
- 每台电脑生成一次性 Chrome 自动配对凭据；成功使用后立即删除。
- 文件尚未稳定时明确显示“等待自动重试”，约 2–3 秒内自动继续；已导入任务不能再被手动重试改成“重复跳过”。
- 升级数据库会把此前“同一任务已成功但误标重复”的记录自动恢复为“导入成功”。
- 自动重试设有硬上限：文件稳定检查最多 20 次，其他临时错误最多 12 次，Eagle 等待最多约 1 小时。
- 同一网页的重复来源事件会先合并，再与其他网页比较；不同网页确实无法区分时仍保持来源为空。
- 主程序、冻结后台、托盘、窗口、安装器和 Chrome 扩展统一使用“下载中转站”名称与图标。
- 日常运行使用 `下载中转站后台.exe`，不再启动开发机 `pythonw.exe`；任务管理器文件说明与产品名均为“下载中转站”。
- 主界面与托盘菜单提供检查更新；每天最多静默检查一次，用户确认后才安装。
- 更新清单使用离线私钥签名，客户端内置公钥验签，并核对 ZIP 的 SHA-256、大小和 GitHub 仓库地址。
- 更新前暂存旧程序目录，新版本需通过 20 秒本机健康检查；隔离测试已确认故障时恢复旧版本和配对信息。
- Chrome 扩展每 30 分钟低频确认一次后台版本，只在后台版本更新时自行重载。
- 源码已发布到 `Ly233ly/download-for-eagle`；当前发行统一采用 GPL-3.0，历史 MIT 通知继续保留。运行数据、配对信息、私钥和发行包不进入 Git。
- 89 项自动测试、Eagle 4 新旧健康接口、扩展连接健康端点、配对竞态与共享状态串行化、真实帧内存边界、任务预览/输出路径归属、稳定播放器归组、自适应清单与质量目录、传输分片折叠、补丁升级自重载、首次缓存快照、统一归组计数、全媒体 `desktop` 路由、普通直链本机下载、仅下载、受保护分轨、AES-128 HLS、schema 5 迁移、所有 JavaScript 语法、Chrome/Firefox 清单和上游对照均通过。
- 公开 Apple HLS 与 B 站 DASH 实际拉流、5 秒 streamcopy 合并及 ffprobe 音视频流检查通过。
- 当前电脑 Eagle 4.0.0 Build 20250917 的 V1 健康回退和助手“Eagle 已连接”状态已实机通过。
- schema 5 新载荷已在当前电脑覆盖安装；冻结后台、安装器健康门、关键文件哈希、Eagle 连接、桌面媒体任务页和重启后任务保留均通过。
- 1.1.1 最终 ZIP 为 93,305,651 字节，SHA-256 `2c5650163ebb6b758486f28ef7e34b1640fcb3205b95673e109e7d6f4fdecc14`；覆盖安装后旧根 `background.js`、`popup.js` 和 `media-control.js` 均已清除。
- 真实 IDM 下载、真实 Eagle 导入和 Behance 来源写入此前已验证成功。

## 仍需接收者手动完成

- Chrome 出于安全限制要求用户亲自开启开发者模式并选择“加载已解压的扩展程序”。安装器会自动打开页面和正确文件夹，加载后无需再输入配对码。
- 在扩展工具栏里确认分片默认隐藏、首次打开定位最新项，并对一个 `completed_local` 任务点“导入现有文件”完成 Eagle 闭环。
- 若 IDM 的“病毒扫描程序”已被安全软件占用，安装器不会覆盖，需要用户决定是否改用该入口。

## 已知边界

- IDM 不提供来源网页；浏览器未识别到的下载会按无来源直接导入。
- 未发布到 Chrome 应用商店，Chrome 会显示正常的开发者模式扩展提示。
- 0.5.0 及更早版本需手动运行一次当前安装器；覆盖 1.2.0 后需在 `chrome://extensions` 手动重载一次已加载的解压扩展并刷新来源页。
