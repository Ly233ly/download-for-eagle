# 任务列表

状态：`[x]` 完成，`[-]` 已实现待人工复验，`[ ]` 未完成。

## 核心与流程

- [x] IDM 完成事件快速写入持久队列并立即唤醒助手。
- [x] 无来源视频直接导入；旧“等待来源”任务自动迁移。
- [x] 有可靠来源时保存网页地址，不猜测来源。
- [x] 已导入项目可以事后补写来源，不重复导入。
- [x] Eagle 离线等待、自动重试和永久失败上限。
- [x] 保留原文件，不移动、不删除、不修改。
- [x] 非视频过滤、文件稳定性检查和完整内容去重。
- [x] 合并短时间内同一路径的重复 IDM 通知。

## Chrome 与网站规则

- [x] Manifest V3 扩展、网站开关、子域名规则和默认关闭。
- [x] 自动捕获下载点击与视频请求，支持手动记录网页。
- [x] 网站关闭或用户单次忽略时发送明确跳过事件。
- [x] 配对码、扩展来源、随机令牌和离线事件队列。
- [x] 配对状态持久化、POST 认证兼容和安装器一次性自动配对。
- [x] 同一网页的重复视频请求先合并，避免可靠来源因自身重复而被判为不确定。

## 界面、安装与资源

- [x] 托盘图标、记录窗口、重试、打开路径/来源、补写来源、诊断导出和清理。
- [x] 移除会触发 `0xc0000409` 的 Python 托盘回调，改用独立 Windows Forms 托盘宿主。
- [x] 托盘宿主到助手的打开、检查、退出、重启和单实例控制链通过验证。
- [-] 用户标出的系统任务栏图标物理右键，待用户确认菜单显示且不闪退。
- [x] 来源可选提示和无来源导入结果说明。
- [x] 窗口隐藏时降低刷新频率，任务列表无变化时不重复重建。
- [x] 下载跨进程唤醒，空闲时降低数据库轮询频率。
- [x] 不写死 Python 3.14，自动寻找常见 Python 3.11+ 安装位置。
- [x] 项目根目录提供 `下载中转站.exe`，IDM 使用轻量 `launcher\IdmEagleHook.exe`。
- [x] 主程序、冻结后台、托盘、窗口、安装器和 Chrome 扩展统一使用自有名称与图标。
- [x] 正常运行不再出现本助手的 `pythonw.exe`，任务管理器显示“下载中转站”。
- [x] 保存 IDM 安装前原值：程序路径与参数均为空。
- [x] 网站规则列表、右下角快捷入口和失败/处理中状态汇总。
- [x] IDM 触发助手静默自启动；浏览器来源快速补发和 4 秒来源宽限。
- [x] PyInstaller 独立运行后端，发行包不依赖接收者安装 Python。
- [x] 一键安装器自动复制、配置 IDM、创建快捷方式、注册卸载并打开 Chrome 安装引导。
- [x] 已有杀毒软件配置保护、原设置备份和卸载恢复。
- [x] 每台电脑独有的一次性 Chrome 自动配对凭据，使用后销毁。
- [ ] 可选的 Windows 开机启动；执行前需用户明确决定。

## 验证与交付

- [x] 44 项自动测试和端到端模拟通过。
- [x] 真实 IDM 文件路径传递和真实 Eagle 导入成功。
- [x] 真实 Behance 来源网页成功写入 Eagle。
- [x] 旧等待记录已在升级助手后无来源导入，Eagle 素材与空来源字段均已核对。
- [ ] Eagle 关闭后下载，再启动 Eagle 自动补导入。
- [ ] 同内容重复下载、两条并发下载、网站关闭和非视频的真实测试。
- [x] 自动重试明确提示、2–3 秒文件稳定重试、完成任务重试保护和旧误报记录迁移。
- [x] 自动重试硬上限：文件稳定 20 次、临时错误 12 次、Eagle 等待约 1 小时。
- [x] README、验收、架构、安装、回滚和开发文档与 0.5.0 同步。
- [x] 隔离目录完成一键安装、独立启动、IDM 接收和自动配对真实验收。
