# 29 任务事实源恢复

状态：完成  
验收：A125

## 目标

popup 重新打开、桌面下载完成或本机连接短暂中断后，仍以 SQLite `download_plans` 为唯一任务事实源，不保留误导性的创建快照。

## 实现与验收

- `completed_local` 与 `imported` 视图强制为 100%。
- 方案列表轮询成功后恢复连接灯；失败时显示同步中断并继续重连。
- HTTP 401 清除失效令牌；成功覆盖升级轮换新的单次 bootstrap，无法恢复时明确进入配对态。
- Node 任务视图测试、扩展静态连接测试、88 项全量回归和当前电脑 1.1.4 健康门通过。
