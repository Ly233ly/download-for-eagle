# 任务 16：扩展弹窗 UI 与交互逻辑重构

## 目标

删除当前新旧两套 popup 并存的结构，用一套内容级媒体、任务和工具界面承载全部 cat-catch 能力；修复跨内容配对、整页重绘、全局单任务和动作语义混乱。

设计与交互真相来源：[`docs/EXTENSION_UI_REDESIGN.md`](../EXTENSION_UI_REDESIGN.md)。

## 工作包

- [x] 16.1 获取当前 popup 实机截图，完成问题审计。
- [x] 16.2 生成三套独立视觉方向，由用户选定方案 2。
- [x] 16.3 抽离候选规范化、内容归组、推荐和动作可用性纯函数。
- [x] 16.4 建立单一 popup store，区分连接、候选组、逐组选择、草稿、多任务与工具状态。
- [x] 16.5 重建媒体页、内容组版本选择、安全筛选和显式批量模式。
- [x] 16.6 重建任务页，支持多任务恢复、进度、错误和停止。
- [x] 16.7 把深度搜索、缓存、录制和移动 UA 重排到设置内的高级捕获；自动下载不进入新版界面。
- [x] 16.8 把网站来源、Eagle、筛选和兼容设置重排到设置/高级操作；外部下载目标退出主流程。
- [x] 16.9 删除第二套 UI 与隐藏上游运行时 DOM；新版 UI 直接调用后台/内容脚本能力。
- [x] 16.10 修复字幕未进入计划和 B 站新版封面链路，并补回归测试；旧浏览器/在线合并动作由任务 17 删除。
- [x] 16.11 记录旧 Chrome 受保护 CDN 临时规则仍可能返回 403；该路线由任务 17 删除，不再作为现行实现。
- [x] 16.12 修复顶部“助手离线”误报：连接检查改用后端实际存在且返回标准数据包的 `/api/media/health`。
- [x] 16.13 带请求上下文的分轨由认证本机助手直接拉取并无损合并；真实双轨 HTTP 测试强制校验 Referer/User-Agent。
- [x] 16.14 修复任务进度与 Eagle 导入状态分裂、重开列表为空和旧监听器抢答；计划与 job 在导入终态原子同步，旧数据读取时自愈。
- [x] 16.15 修复通用站点使用 favicon 冒充视频图、首次打开列表为空和工具栏/列表数量不一致；增加跨 frame 内容图、当前页截图兜底、缓存快照门和统一归组计数。
- [ ] 16.16 schema 5 最终打包、覆盖安装和文档证据已完成；Chrome 最终实机视觉 QA 待人工截图。

## 验收

- [x] A96 单界面与单状态源（自动结构检查通过）。
- [x] A97 内容级归组（纯函数测试通过）。
- [x] A98 可见选择与路由（提交前校验与路由测试通过）。
- [x] A99 多任务与恢复（plans 恢复、聚合轮询与逐任务停止已实现）。
- [x] A100 功能对等重排（唯一入口与上游兼容运行时检查通过）。
- [ ] A101 视觉与交互 QA。
- [x] A102 回归与交付（schema 5 源码 83 项；1.1.0 的正式构建、冻结运行时、隔离安装/回滚、覆盖安装、健康与载荷哈希已通过；1.1.1 补丁复验见独立报告）。
- [x] A103 受保护 CDN 的 Chrome 实际发起、本机拉流合并与 Eagle 导入（用户实机任务已显示导入成功）。
- [x] A104 跨站可识别内容图且不使用 favicon 冒充（纯逻辑、结构和脚本测试通过）。
- [x] A105 首次快照、自动刷新和工具栏/列表计数一致（延迟恢复与归组测试通过）。

所有标准的可执行定义见 UI 重构规范。A96–A105 未全部通过前，不生成新的正式发布结论。
