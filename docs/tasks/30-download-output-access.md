# 30 仅下载结果可达

状态：完成  
验收：A126

## 目标

“仅下载”完成后直接告诉用户文件在哪里，并从任务卡打开所在目录。

## 实现与验收

- 任务卡显示数据库中的最终完整路径和“打开所在文件夹”。
- 客户端只发送 `planId`；服务端解析 `final_path`，要求文件真实存在且位于 `下载中转站/已完成`。
- 任意路径、目录逃逸、已删除文件或非本程序文件均拒绝。
- 协调器单元测试和认证 HTTP 接口测试均覆盖成功与越界拒绝。
